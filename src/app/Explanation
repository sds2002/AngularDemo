fields: string[] → list of properties to check (name and role).

some() → returns true if any field matches the search text.

?.toString() → ensures it works even if property isn’t a string.

pure: false → re-runs whenever search input changes.