What Happens?

If you click Test Drive on Toyota Corolla →
→ calls Car.drive() →
→ "Toyota Corolla is driving normally 🚗"

If you click Test Drive on Tesla Model 3 →
→ calls overridden method ElectricCar.drive() →
→ "Tesla Model 3 is driving silently ⚡"

✅ This is runtime polymorphism in action:
Even though the array is typed as Car[], at runtime Angular/TypeScript dispatches the actual method depending on the object’s type (Car vs. ElectricCar).