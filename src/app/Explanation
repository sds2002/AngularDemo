What is AsyncPipe?

Angular’s AsyncPipe is used to subscribe to Observables or Promises directly in templates.

It automatically subscribes and unsubscribes → prevents memory leaks.

It also updates the view whenever new data is emitted.

🛠 Hands-on Example: AsyncPipe

We’ll show both:

Observable (interval)

Promise (delayed response)