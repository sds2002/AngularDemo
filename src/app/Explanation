Key Takeaways

Interfaces + HTTP: Ensures API response is typed correctly.

Observable subscription: Handles async data safely.

Error handling: Using subscribe({ next, error }) to manage API failures.

Scalable: Easy to extend User interface if API adds more fields.